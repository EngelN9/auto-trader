# Trading runtime

Server-side lifecycle skeleton. It validates research configuration and emits a structured heartbeat
while remaining halted. It does not depend on the dashboard and cannot submit external orders.
