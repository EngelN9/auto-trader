# Control API

FastAPI control-plane boundary. Milestone 0 exposes only health and mock system status queries.
It contains no authentication implementation, command endpoints, or broker-order endpoint.
