{
  "name": "auto-trader-monorepo",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.7.0",
  "scripts": {
    "build": "pnpm --filter dashboard-web build",
    "lint": "pnpm --filter dashboard-web lint",
    "test": "pnpm --filter dashboard-web test",
    "typecheck": "pnpm --filter dashboard-web typecheck"
  }
}
